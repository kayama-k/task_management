spring.application.name=backend

# PostgreSQL (backend/compose.yaml). Connectivity only for now: no JPA
# entities/tables yet, so no ddl-auto/Hibernate settings here.
spring.datasource.url=jdbc:postgresql://localhost:5432/taskmanagement
spring.datasource.username=taskmanagement
spring.datasource.password=taskmanagement
spring.datasource.driver-class-name=org.postgresql.Driver
